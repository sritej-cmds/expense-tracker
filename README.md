# Expense Tracker

Shared expense tracker — React + Vite + TS frontend, FastAPI + Pydantic backend, PostgreSQL DB.

## Structure
- `backend/` — FastAPI app (MVC: core, models, schemas, controllers, services)
- `frontend/` — React + Vite + TS app

## Setup
See team build plan PDF for full setup steps per module.
